# 快速开始

## 🎯 管理员：创建新用户

```bash
cd /Users/lisicheng/mac-multi-user-setup

# 创建用户（使用默认密码 user2026）
sudo ./scripts/quick-create-user.sh 用户名

# 或指定密码
sudo ./scripts/quick-create-user.sh 用户名 自定义密码

# 或指定密码和 API key
sudo ./scripts/quick-create-user.sh 用户名 密码 sk-ant-api03-xxxxx
```

**示例：**
```bash
sudo ./scripts/quick-create-user.sh zhangsan
sudo ./scripts/quick-create-user.sh lisi lisi123
sudo ./scripts/quick-create-user.sh wangwu ww2026 sk-ant-xxxxx
```

## 📋 给新用户的信息

创建用户后，将以下信息发送给用户：

```
本地地址: 192.168.10.118
外网地址: 188.253.4.121（端口 2222）
用户名: xxx
密码: xxx

连接命令:
  本地: ssh xxx@192.168.10.118
  外网: ssh -p 2222 xxx@188.253.4.121

使用指南: 新用户使用指南.md
```

## 👤 用户：首次使用

```bash
# 1. 连接到服务器（本地）
ssh 你的用户名@192.168.10.118

# 或从外网连接
ssh -p 2222 你的用户名@188.253.4.121

# 2. 修改密码（建议）
passwd

# 3. 启动 tmux
tmux new -s main

# 4. 配置 Claude（如果需要）
claude config

# 5. 启动 Claude
claude

# 6. 使用 OpenClaw（可选）
openclaw onboard
```

## 📚 完整文档

- **新用户使用指南.md** - 给用户的完整使用说明
- **管理员快速参考.md** - 管理员常用命令和操作
- **docs/remote-access.md** - 远程访问详细配置
- **README.md** - 项目完整文档

## 🔗 当前服务器信息

- **本地 IP**: 192.168.10.118
- **公网 IP**: 188.253.4.121
- **SSH 端口**: 22（本地）/ 2222（外网）
- **已创建用户**: yuanboyang

## ⚡ 常用命令

```bash
# 查看所有用户
dscl . list /Users | grep -v "^_"

# 查看登录用户
who

# 查看 Claude 进程
ps aux | grep claude

# 重启 SSH 服务
sudo launchctl unload /System/Library/LaunchDaemons/com.openssh.sshd.plist
sudo launchctl load -w /System/Library/LaunchDaemons/com.openssh.sshd.plist
```
